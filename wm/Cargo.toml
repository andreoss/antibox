[package]
name = "antibox-wm"
version = "1.1.0"
edition = "2018"

[dependencies]
antibox-core = { path = "../core" }
antibox-ui = { path = "../ui" }

[dev-dependencies]
antibox-x11 = { path = "../x11" }

[features]
default = ["tray"]
tray = []
